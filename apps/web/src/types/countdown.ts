export enum CountdownStatus {
  STARTED = "Started",
  STOPPED = "Stopped",
}

export enum CountdownType {
  Standard = "standard",
  FastMode = "fastMode",
  HurryMode = "hurryMode",
}

export enum CountdownTimer {
  Standard = 3,
  FastMode = 10,
  HurryMode = 5,
}
