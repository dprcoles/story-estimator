export enum CountdownStatus {
  STARTED = "Started",
  STOPPED = "Stopped",
}

export enum CountdownType {
  Standard = "standard",
  FastMode = "fastMode",
}

export enum CountdownTimer {
  Standard = 3,
  FastMode = 10,
}

